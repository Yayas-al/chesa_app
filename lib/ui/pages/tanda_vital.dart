import 'package:flutter/material.dart';
import '../../shared/theme.dart';

class tandaVital extends StatelessWidget {
  const tandaVital({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
